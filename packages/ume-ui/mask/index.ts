import UMask from './src/UMask.vue';
import { withInstallComponent } from '@/utils/install';

export const UMaskComponent = withInstallComponent(UMask);
export default UMaskComponent;

export { UMask };
